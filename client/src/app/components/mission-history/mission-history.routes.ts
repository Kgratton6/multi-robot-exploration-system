import { Routes } from '@angular/router';
import { MissionHistoryComponent } from './mission-history.component';

export const MISSION_HISTORY_ROUTES: Routes = [
    {
        path: '',
        component: MissionHistoryComponent
    }
];